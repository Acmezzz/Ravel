# 2026-08-28

- 对 Ravel（apps/ravel-desktop + docs/ 五份活文档）做了一次全面代码审查，报告在 `.workbuddy/artifacts/code-review-2026-08-28.md`。
- 关键发现：本机 PortableGit 2.55.0.windows.3 存在 `git update-ref` 对三段式 ref 静默 no-op 的回归，导致 checkpoint 功能失效、3 个测试失败；`createCheckpoint` 缺 update-ref 事后校验放大了该问题。桌面套件实际 281 过 / 3 挂 / 1 取消（文档声称 285 过）。
- 其余：`omega:bash` 用户直发命令审批不留痕（P2）；探索性凝练草稿全量永久落盘无回收（P2）；`revisionMatches` 零 evidence 放行（P2）；文档 §9 落点漂移（components/histos 实际在 components/panels）；gitignore 漏 release3/（13 个 release 目录 >1GB）。
- 契约吻合度总体很高：单写者、审批 fail-closed、内容寻址工件、IPC 四方同步、CSP/IIFE/asar 等硬约束均核实通过。
- 以 software-company 团队（software-ravel-gap）完成 7 主流 Agent（ZCode/Claude Code/Devin/omp/Codex/Hermes/Manus）竞品调研 + Ravel 功能盘点 + 围绕 Histos 的差距分析。产出三份报告在 `.workbuddy/artifacts/`：`histos-gap-analysis-2026-08-28.md`（总）、`competitive-analysis-7-agents.md`、`ravel-feature-inventory.md`。
- 关键结论：Histos「画布+工件语义图+受控执行」组合全网唯一，差异化窗口真实；但 semanticProvider 未接入锁死所有语义能力（唯一 P0）；修正了 PM 误判（多档权限已落地）；核心机会是把标配 Histos 化：Plan-as-Artifact、定时 Flow、触发式检索、skill 蒸馏、图回放、实据完成判据。
