本轮完成 Histos 代码审查后的最小清理与状态文档重写，并清理路线图删除后的两处残余引用：修复 suggestContext 查询按空白分词的正则错误，移除 Web source 未使用 workspaceId，补齐 suggestContext/importContext worker 协议方法；更新 docs/README.md、docs/ravel-histos-next-cycle.md、apps/ravel-desktop/docs/implementation-status.md，使其准确区分生产已接入与仅原语/dry-run能力。根 npm run check、Histos context/Web 定向测试 32/32、renderer typecheck、三处 Electron syntax 均通过；桌面全量测试在整理提交后复核仍为 442 项中 439 通过、3 个 checkpoint PortableGit ref 持久化失败；该问题仍未解决。

桌面端建设第一批（40a298188）：新增右栏 agent 面板接线 histosListCapabilities / histosInvokeNode（dry-run 默认开）/ flowScheduleList / flowScheduleRemove；GraphPanel 定时参数可配置并新增 histosApplyWebResources 导入与 histosRebuild 两步确认重建；右 rail 补齐 agent/telemetry/snapshots/terminal 图标。新增面板须同时改 useAppStore rightTab、RightPanel isRightTab/tabs/render 四处；加载/错误/空态沿用 omega-spinner、role=alert+omega-error-text、omega-graph-empty 约定。Vite build 曾因沙箱 safe-delete 拦截 dist 清理失败，手动清 dist 后通过。下一批候选：listAgentRuns channel、Flow 启停与触发历史、node/artifact/diff 视图、eval 产生器。